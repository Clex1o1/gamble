import { describe, it, expect } from 'vitest'

describe('Guess Service', () => {
    it('should allow the player to guess "up"', () => {
    })
    it('should allow the player to guess "down"', () => {
    })
    it('should not allow a new guess if a guess is already pending', () => {
    })
    it('should allow a new guess only after 60 seconds and a price change', () => {
    })
    it('should not allow a new guess if the price has not changed after 60 seconds', () => {
    })
    it('should increase the score by 1 for a correct guess', () => {
    })
    it('should decrease the score by 1 for an incorrect guess', () => {
    })
})