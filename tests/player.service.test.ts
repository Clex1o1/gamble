import { describe, it, expect } from 'vitest'
describe('Player Service', () => {
    it('should initialize a new player with a score of 0', () => {
    })
    it('should allow the player to make a guess', () => {
    })
    it('should not allow the player to make a new guess if a guess is already pending', () => {
    })
    it('should persist the player’s score across sessions', () => {
    })
    it('should restore the player’s score after session reload', () => {
    })
})


