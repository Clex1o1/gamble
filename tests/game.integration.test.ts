import { describe, it, expect } from 'vitest'

describe('Game Integration', () => {
    it('should allow a player to play a full round: make a guess, wait, resolve, and update score', () => {
    })
    it('should block new guesses until the previous one is resolved', () => {
    })
    it('should persist and restore player state across sessions', () => {
    })
})
